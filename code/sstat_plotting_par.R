##  sstat plotting parameters
plot.pars <- list(
	# to plotting device
	width <- 3.75,
	height <- 3.75,
	
	# to `par'
	mar <- c(3,3,0,0) + 0.1,
	mgp <- c(2,1,0),
	
	# to `text' for labels
	adj <- c(1.5,1.5),
	cex.txt <- 1.5
)